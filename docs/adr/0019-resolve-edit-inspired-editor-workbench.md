# Resolve Edit-inspired editor workbench

Editor-next should move toward a Resolve Edit-inspired **Editor workbench**: a compact inspector area for media and export context, central preview, transport controls, and a lower selection/waveform surface. The workbench should not add a persistent icon/status rail unless it has a clear navigational or state-changing function. This keeps the UI feeling like a professional media editor while preserving YAFFW's single-asset model: no bins, source monitors, clip stacks, project timeline, or multi-asset composition.

**Consequences**

- The first-slice card/page layout is treated as prototype scaffolding, not the long-term product shape.
- The workbench may borrow professional editor composition and density, but domain language stays centered on **Media asset**, **Selection**, **Playhead**, **Waveform lane**, **Export review**, and **Delivery action**.
- Waveform lane identity belongs inside each lane row using media-track names such as voice or desktop, not in a separate V1/A1/A2-style track console.
